return {
    target = function() 
        return "THE_TARGET"
    end,
    init = function(self, ctx)
        return {
            plugin_info = "PLUGIN_STUB",
            context = ctx
        }
    end
}